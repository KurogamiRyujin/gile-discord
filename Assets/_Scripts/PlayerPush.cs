using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerPush : MonoBehaviour {

	void OnCollisionEnter2D(Collision2D other) {
//		Vector2 pos = Vector2.zero;
//		if (Input.GetAxis ("Horizontal") != 0f && other.collider.gameObject.layer == LayerMask.NameToLayer ("Pushable")) {
//			pos = other.transform.position;
//			other.transform.Translate (Input.GetAxis("Horizontal") * 5f * Time.deltaTime, 0, 0);
////			Debug.Log (pos);
//		}
	}
}
